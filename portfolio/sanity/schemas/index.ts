import project from './project';
import post from './post';
import siteSettings from './siteSettings';
import skillCategory from './skillCategory';

export const schemaTypes = [project, post, siteSettings, skillCategory];
